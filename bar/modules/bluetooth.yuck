(defpoll bluetooth :interval "5s" "~/.config/eww/bar/scripts/bluetooth.sh")

(defwidget bluetooth []
  (box :class "module bluetooth" :spacing 4
    (label :text "󰂯")
    (label :text "${bluetooth}")))
