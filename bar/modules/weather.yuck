(defpoll weather :interval "600s" "~/.config/eww/bar/scripts/weather.sh")

(defwidget weather []
  (box :class "module weather" :spacing 4
    (label :text "${weather.icon}" :class "weather-icon")
    (label :text "${weather.temp}")))
